name: Sync Fork Release

on:
  workflow_run:
    workflows:
      - Publish GHCR image
    types:
      - completed

permissions:
  contents: write

concurrency:
  group: sync-fork-release-main
  cancel-in-progress: false

jobs:
  sync-release:
    # Only publish a release after the fork's own main-image build succeeds.
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout published source
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0

      - name: Read release version
        id: version
        shell: bash
        run: |
          VERSION="$(tr -d '[:space:]' < backend/cmd/server/VERSION)"
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "publishable=false" >> "$GITHUB_OUTPUT"
            echo "No SemVer VERSION on this main commit; image publishing will not create a release."
            exit 0
          fi
          echo "publishable=true" >> "$GITHUB_OUTPUT"
          echo "value=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Skip an existing release
        id: existing
        if: steps.version.outputs.publishable == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Release $TAG already exists; no duplicate release will be created."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Build fork release notes
        if: steps.version.outputs.publishable == 'true' && steps.existing.outputs.exists != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
          VERSION: ${{ steps.version.outputs.value }}
        run: |
          if ! gh api "repos/Wei-Shaw/sub2api/releases/tags/$TAG" --jq .body > upstream-release.md; then
            printf '> AI API Gateway Platform - 将 AI 订阅配额分发和管理\n' > upstream-release.md
          fi

          python3 - <<'PY'
          import os
          from pathlib import Path

          body = Path("upstream-release.md").read_text(encoding="utf-8").strip()
          for marker in (
              "\n---\n\n## 📥 Installation",
              "\n## 📥 Installation",
              "\n---\n\n## Installation",
              "\n## Installation",
          ):
              index = body.find(marker)
              if index >= 0:
                  body = body[:index].rstrip()
                  break

          version = os.environ["VERSION"]
          repo = os.environ["GITHUB_REPOSITORY"]
          image = f"ghcr.io/{repo.lower()}"
          images = "\n".join([
              f"## Docker 镜像（{image}）",
              "",
              "```text",
              f"{image}:latest",
              f"{image}:{version}",
              "```",
              "",
              "拉取示例：",
              "",
              "```bash",
              f"docker pull {image}:{version}",
              "```",
              "",
              "## 文档",
              "",
              f"- [GitHub Repository](https://github.com/{repo})",
              f"- [Deployment Guide](https://github.com/{repo}/tree/main/deploy)",
          ])
          Path("release-notes.md").write_text(
              f"{body}\n\n---\n\n{images}", encoding="utf-8"
          )
          PY

      - name: Create fork release
        if: steps.version.outputs.publishable == 'true' && steps.existing.outputs.exists != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
          VERSION: ${{ steps.version.outputs.value }}
          TARGET: ${{ github.event.workflow_run.head_sha }}
        run: |
          gh release create "$TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$TARGET" \
            --title "Sub2API $VERSION" \
            --notes-file release-notes.md
